{
  "framework": "nextjs",
  "buildCommand": "prisma generate && next build",
  "installCommand": "npm install",
  "env": {
    "DATABASE_URL": "file:/tmp/dev.db",
    "NEXT_PUBLIC_BASE_URL": "https://million-image-gallery.vercel.app"
  }
}
